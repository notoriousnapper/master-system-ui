const LIST_CONFIG = { // TODO: Move this to a file locally
    container: {
        name: "testConfig",
            className: "list"
    },
    adapter: {
    },
    node: {
        nodeCount : 3, // means expect 3 different types of
            isStrict : true,
            labelName: "",
            hasLabel: false
    }
};